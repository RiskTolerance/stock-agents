// This file is kept for backward compatibility but is no longer used
// The scheduler now uses a simple heartbeat system instead of complex schedules
// See scheduler/index.ts and scheduler/heartbeat.ts for the new implementation

export {};
